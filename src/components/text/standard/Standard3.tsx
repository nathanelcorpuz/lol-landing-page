export default function Standard3({ children }: { children: React.ReactNode }) {
	return (
		<p className="text-grey-1 text-sLarge leading-[83px] tracking-[1.9px] italic font-medium font-beaufort">
			{children}
		</p>
	);
}
