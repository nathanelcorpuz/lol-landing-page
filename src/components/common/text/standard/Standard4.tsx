export default function Standard4({ children }: { children: React.ReactNode }) {
	return (
		<p className="text-grey-1 text-sStat leading-[27px] tracking-[0.9px] font-beaufort font-bold">
			{children}
		</p>
	);
}
