export default function Standard2({ children }: { children: React.ReactNode }) {
	return (
		<p className="text-sBol leading-[21px] tracking-[0.7px] font-spiegel font-bold">
			{children}
		</p>
	);
}
